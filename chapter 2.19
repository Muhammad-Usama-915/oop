#include<iostream>
using namespace std;
int main()
{
	float a,b,c;
	
	cout<<"Enter integer one";
	cin>>a;
	cout<<"Enter integer two";
	cin>>b;
	cout<<"Enter integer three";
	cin>>c;
system("cls");
//sum,average,product,smallest,largest we have to solve now

cout<<"Input three different integers:"<<a<<","<<b<<","<<c<<","<<endl;
cout<<"Sum:"<<a+b+c<<endl;
cout<<"Product:"<<a*b*c<<endl;
cout<<"average"<<a+b+c/3<<endl;
//now larger case
if(a>b&&a>c)
cout<<a<<"so a is larger";
if(b>a&&b>c)
cout<<b<<"so b is larger";
if(c>a&&c>b)
cout<<c<<"so c is larger";
//smaller same as for large
if(a<b&&a<c){
cout<<a<< "is samller";
}

if(b<a&&b<c){
	cout<<b<<"is smaller";
}

if(c<a&&c<b){
	cout<<c<<"is smaller";
}
return 0;
}
